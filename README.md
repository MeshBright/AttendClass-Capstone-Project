# AttendClass-Capstone-Project
AttendClass is an offline-first, edge-computing attendance server built on a dual-core FreeRTOS architecture. It eliminates proxy attendance by combining strict biometric Two-Factor Authentication with a custom Bluetooth geofence, processing up to 25 authentications per minute with zero UI latency—completely independent of local Wi-Fi stability
